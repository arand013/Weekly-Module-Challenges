
// Identify the length are the same 
// Check and coount the ooccurence of each character () Hash Map
// 1 hashmap for each string 

var isAnagram = function(s, t) {

    if(s.length !== t.length) return false;

    for(i = 0; i < s.length; i++){
        hash.set(s[i], t[i]);

    }
       
    
};